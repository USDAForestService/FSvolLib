{
  "name": "@fsvollib/js-test",
  "version": "0.1.0",
  "private": true,
  "description": "Smoke tests for FSvolLib WASM build pipeline.",
  "type": "module",
  "dependencies": {
    "@fsvollib/js": "file:../FSvolLibJS"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8"
  }
}
